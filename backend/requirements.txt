# FastAPI and related packages
fastapi>=0.111.0,<0.113.0        # Compatible with pydantic>=2.0,<3.0 and starlette>=0.37.2,<0.39.0
uvicorn>=0.23.0,<0.29.0
pydantic>=2.0,<3.0

# Testing
pytest>=7.0.0,<8.0.0
pytest-asyncio>=0.21.0,<0.22.0
pytest-cov>=2.12.1,<2.13.0
httpx>=0.23.0,<1.0.0              # NOTE: 1.0.0+ requires Python 3.10+, so capped for Py 3.9

# NLP and AI
transformers==4.39.3
tokenizers>=0.14,<0.20
torch<=2.7.0                     # Optional upper bound to avoid breaking change
openai>=1.1.0,<1.3.0             # Verified working with pydantic >=2.0,<3.0
llama-index==0.9.48             # Matches transformers and openai 1.x
python-dotenv>=0.19.0,<0.20.0

# Utilities
requests>=2.31.0,<3.0.0          # Required by both transformers and llama-index
numpy>=1.23.0,<1.27.0
pandas>=1.3.0,<1.4.0

# CORS and Starlette
starlette>=0.37.2,<0.39.0        # Verified compatible with FastAPI >=0.111

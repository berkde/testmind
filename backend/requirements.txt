# FastAPI and related packages
fastapi>=0.111.0,<0.113.0        # Compatible with pydantic>=2.0,<3.0 and starlette>=0.37.2,<0.39.0
uvicorn>=0.29.0,<0.30.0          # Latest stable, supports Python 3.11/3.12
pydantic>=2.7.0,<3.0             # v2+ required for FastAPI >=0.110
pydantic-settings>=2.2.1,<3.0    # For config management, matches pydantic v2

# Testing
pytest>=8.0.0,<9.0.0             # Latest stable
pytest-asyncio>=0.23.0,<0.24.0   # Latest, supports Python 3.11+
pytest-cov>=4.1.0,<5.0.0         # Latest, for coverage
httpx>=0.27.0,<1.0.0             # 1.0.0+ is still in beta, 0.27+ is stable

# NLP and AI
transformers==4.39.3             # Stable, matches torch 2.x
llama-index==0.9.48              # Matches transformers and openai 1.x
tokenizers>=0.14,<0.19           # transformers 4.39.3 requires <0.19
torch>=2.1.0,<2.8.0              # 2.7.0 is latest, 2.1+ is stable for most AI libs
openai>=1.12.0,<2.0.0            # 1.12+ for pydantic v2 support
python-dotenv>=1.0.1,<2.0.0      # 1.0+ is latest, supports Python 3.11+

# Utilities
requests>=2.31.0,<3.0.0          # Required by both transformers and llama-index
numpy>=1.26.0,<2.0.0             # 1.26+ for Python 3.11+
pandas>=2.2.0,<3.0.0             # 2.2+ for Python 3.11+

# CORS and Starlette
starlette>=0.37.2,<0.39.0        # Verified compatible with FastAPI >=0.111
